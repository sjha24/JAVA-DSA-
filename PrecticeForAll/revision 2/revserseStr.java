import java.util.Scanner;
public class revserseStr {
    public static void main(String[] args) {
        Scanner scn = new Scanner(System.in);
        String str = scn.next();
        for(int i = str.length()-1; i>=0;i--){
            char ch = (char)(str.charAt(i));
            if(str == ch){

            }
        }
    }
}
